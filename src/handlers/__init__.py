from . import start
from . import cards
from . import settings

users = {
    'start': start.start,
    'lang_ru': settings.set_locale_ru,
    'lang_en': settings.set_locale_en,
    'lang_kaz': settings.set_locale_kaz,
}

admins = {

}
